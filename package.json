{
    "scripts": {
      "heroku-prebuild": "cd src/Assignment2/cardlist && npm install && npm run build && cd Assignment3 && npm install",
      "start": "cd src/Assignment2/cardlist && npm start"
    }
  }